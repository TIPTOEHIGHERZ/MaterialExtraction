train_config:
  batch_size: 8
  epochs: 22000
  lr: 0.000001
  device: cuda
  # ckpt_dir: checkpoints/thinplate/last

  summon_ckpt: true
  accumulation_steps: 1
  save_period: 1000
  max_saves: 5
log_config:
  log_dir: thinplate_bn
  log_period_loss: 20
  log_period_visual: 300
